{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build (clang++)",
      "type": "shell",
      "command": "clang++",
      "args": [
        "-std=c++23",
        "-g",
        "-Wall",
        "-Wextra",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
